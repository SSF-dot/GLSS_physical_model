# GLSS_physical_model
GLSS model.cpp
signal pair-to-angle conversion
lg::Signal2Angle()

our proposed physical model
glss::SolvePhyModel()

ideal model and model proposed by Eisert et al.
glss::SolveIdealModel()

camera model
glss::SolveCameraModel();

add noise to the outgoing rays
glss::SolveNoisePhyModel();
